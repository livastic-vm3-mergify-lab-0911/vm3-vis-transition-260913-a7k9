name: VM3 Root2 E2E 9K4M

on:
  pull_request:

permissions:
  contents: read

jobs:
  boundary:
    name: vm3-root2-e2e-boundary-9K4M
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run repository script
        env:
          MERGIFY_TOKEN: ${{ secrets.MERGIFY_TOKEN }}
        run: bash ci/vm3-root2-e2e.sh
